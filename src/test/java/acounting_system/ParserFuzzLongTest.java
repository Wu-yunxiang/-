package acounting_system;

/**
 * 已迁移：长时（5h）Jazzer 模糊测试入口请使用 {@link ParserFuzzLong}。
 *
 * 说明：该文件曾包含 {@code *Test} 命名的 fuzz 用例，会被 Surefire 默认模式自动发现，
 * 导致常规 {@code mvn test}/{@code mvn clean test} 被长时 fuzz 阻塞。
 */
final class ParserFuzzLongTestMigrated {
    private ParserFuzzLongTestMigrated() {
    }
}
